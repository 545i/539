# 更新說明 — 週期化總帳、週導覽、快速上傳強化、快捷帳單(2026-09)

本輪(commits `fa2a86d` → `a8a3258`,分支 `react-rewrite`)的實作總整理。分成
**資料結構/後端**、**每週總帳**、**下注頁週導覽**、**快速上傳**、**帳單輸出** 五塊。

---

## 1. 週期改「全自動」(週一~週日)

- **不再手動開/結算週期**。週期完全由**開獎日期**推導:每 **週一~週日** 為一期,
  跨週自動分期、過了下一週自動另立一期,現有紀錄也立刻自動歸位。
- 週的計算以 **UTC** 回推避開時區把日期挪錯天;週日歸到「前一個週一」。
- 共用工具:`frontend/src/weeks.ts`(`weekMonday` / `isoWeekNo` / `weekRangeLabel` /
  `distinctWeeks` / `weekAddDays`)。
- 設定頁「週期性紀錄」改為**全自動說明卡**(移除手動開始/結算鈕)。
  檔案:`frontend/src/components/views/CycleSettings.tsx`。

**總損益 → 各週損益**(`components/tabs/TotalPnLTab.tsx`)
- 依開獎日期把每筆歸到該週(標「2026/08/24 ~ 08/30」),新→舊排列,底部「全部週合計」。
- **每一週可點開,展開看該週各版(愛甘/阿閔/阿姨…)的損益明細**。
- 原本獨立的「各版損益」表已**移除**(資訊改由各週展開提供)。

---

## 2. 每週總帳(上傳歷史頁)

頁面:`frontend/src/components/views/UploadHistoryView.tsx`(側欄「上傳歷史」),
分兩個分頁:**每週總帳**(預設)與**上傳批次**(原對帳/填回/作廢)。

**每週總帳**(`components/views/WeeklyLedger.tsx`)= 全部下注流水(含快速上傳與手動記錄,
排除模擬版),依開獎日期 週 → 日 → 逐筆:

- **週導覽**:`‹ 上一週 / 下一週 ›`,可切「全部週」列表。
- **按遊戲拆帳**:每日列常駐顯示各遊戲(539/天天樂/六合彩)各自的成本/派彩/盈虧;
  每週展開也有「本週各遊戲」。
- **逐筆明細**:下注方式(含**遊戲徽章 + 版名徽章** + 結果)、下注組合、成本、派彩、盈虧,
  下方算式列 `N車/支 × $每注 = $成本`。派彩取自各筆已結算 record(未開獎顯示「待開獎」)。
- 版篩選:全部版(排除模擬)/ 各版。

---

## 3. 下注頁「週導覽」(五個策略)

共用元件:`frontend/src/components/WeekNav.tsx`(`useWeekNav` hook + `WeekNav` + `WeekSubtotal`)。

- 五個策略(1組/2組、三柱1800碰、9000碰、連碰)的「**02 流水帳**」標題右側都有
  `‹ 第N週(MMDD~MMDD) ›`,`< >` 為**日曆式**方向鍵(不管該週有沒有紀錄都能前後移),
  中間可切「全部週」。標題下方有該週小計(局/注、過關、投入、回收、損益)。
- **統一切換**:五個策略**共用同一個聚焦週**(`WeekFocusProvider`,包在 `main.tsx`),
  在任一頁切週,其他策略頁自動同步。
- **切週時上方儀表板一起算該週**:投入/回收/累積損益/局數/中獎率/總車數/回本試算
  都改吃聚焦週的 `flowRecords`;「全部週」時 = 全部紀錄。

---

## 4. 快速上傳強化(`components/QuickImportModal.tsx`)

- **選擇順序改成 日期 → 版 → 遊戲**。
- **選日期後偵測已上傳的遊戲並標綠**:選好日期(+選定版)後,查上傳歷史,把那天已上傳過的
  遊戲鈕標成**綠色(帶勾)**,上傳成功後自動重查更新。
- **1組 / 2組 支援「單顆號碼加價」**(每注基礎 +N):號碼欄下方輸入 `15:3.5`(多個逗號分隔),
  後端逐顆重算成本。
  - 成本 = 車數 × Σ((每注基礎 + 該號加價)×(num_max−1))。
  - 後端:`backend/routers/importer.py` 的 `CommitItemIn.ball_deltas` + `_recost`(只 single/multi)。
  - 對帳驗證(阿姨 8/29 帳單):15漲3.5、2組 70車 → 該筆 +9,310,二合總計 2,655,858,與帳單一致。

---

## 5. 快捷帳單(每週總帳每日明細)

每日展開有 **「明細 / 快捷」** 切換(`WeeklyLedger.tsx`):

- **明細** = 逐筆表(同上)。
- **快捷** = 對接人帳單,兩種形式:
  - **卡片檢視(閱讀)**:遊戲徽章+日期、**獎號號碼球**、牌支(成本)、**中獎綠字**、
    右上**收/付紅綠上色**、當日累計;依版分區(多版顯示【版名】)。
  - **「複製帳單」**:複製對接人**純文字格式**(貼給組頭)。
- 帳單欄位:`MD / {遊戲}獎號 / 開獎號 / {遊戲}牌支{成本} / {組}中{碰}碰+{派彩} / 共{該遊戲盈虧} / 合計{當日累計}`。
- **中獎碰數換算**:二合 = 中幾顆 × 車數 × 4(例:2組 1顆×17車×4 = 68碰);
  1800碰/連碰/9000碰 直接讀 result「中 X 碰」。
- 帳單**按版分開**(每版一份、合計逐版各自算)。

> ⚠️ 已知小差異:9000碰 的「碰數」目前照紀錄的「中X碰」輸出;對接人帳單有時用「四中0.4碰」
> 那種換算,若要對齊需另定換算規則。

---

## 相關檔案速查

| 功能 | 檔案 |
|---|---|
| 週工具 | `frontend/src/weeks.ts` |
| 週導覽 + 統一切換 | `frontend/src/components/WeekNav.tsx`、`main.tsx` |
| 每週總帳 + 快捷帳單 | `frontend/src/components/views/WeeklyLedger.tsx` |
| 上傳歷史頁(分頁殼) | `frontend/src/components/views/UploadHistoryView.tsx` |
| 各週損益(展開各版) | `frontend/src/components/tabs/TotalPnLTab.tsx` |
| 下注頁儀表板隨週變 | `components/tabs/{GroupBetTab,ThreePillarTab,Combo9000Tab,ComboBetTab}.tsx` |
| 快速上傳(順序/標綠/加價) | `frontend/src/components/QuickImportModal.tsx` |
| 單顆加價(後端算) | `backend/routers/importer.py` |
| 週期設定說明卡 | `frontend/src/components/views/CycleSettings.tsx` |
