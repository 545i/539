pandas>=2.0
scipy>=1.10
questionary>=2.0
plotext>=5.2
rich>=13.0
requests>=2.31
beautifulsoup4>=4.12
pytest>=7.4
openpyxl>=3.1

# ── Web(FastAPI + React 版)──────────────────────────────
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.0
httpx>=0.27          # FastAPI TestClient(API 測試)用

# ── 舊 Streamlit 版(遷移期間保留做回退,切換後可移除)──────
streamlit>=1.40
plotly>=5.20
