# 落差稽核 — 同區 3-4 球(同區極限偏態戰情室)

- 對照:原始 `/tmp/yh_3or4.html`(292 行)/ 規格 `docs/yahong/spec-zone34.md` / 後端 `core/yahong/zone34.py` + `backend/routers/yahong.py` / 前端 `frontend/src/components/views/yahong/Zone34Panel.tsx` + `YahongView.tsx`
- 結論:**核心演算法 100% 忠實移植**,落差全在「呈現層」與「文案」。無 [BUG]。

---

## 演算法核對(全數 OK)

### [OK] consecutiveMiss 計數
四柱分區 `n<=9 / <=19 / <=29 / else`、`max(zones)>=3` 歸零否則 +1、由舊到新掃描 —— 後端 `analyze_zone34`(zone34.py:30-44)與原檔 `calculateAndRender`(html:206-234)逐行等價。`THRESHOLD_HIT=3` 常數一致。
- 資料方向正確:`radar_ep` 傳 `draws_as_lists(load_df(game))`(舊→新,yahong.py:47-49),正是 `analyze_zone34` 期望的方向。原檔用 `sort((a,b)=>a.date>b.date?1:-1)` 同為舊→新。✓

### [OK] 門檻 5 / 7
`THR_A=5`、`THR_B=7`(zone34.py:10-11),`strategy_a = miss>=5`、`strategy_b = miss>=7`,與原檔 `>=5` / `>=7`(html:263,280)一致。兩策略可同時達標。✓

### [OK] 四柱最冷號 findColdest
原檔:`minIdx=+∞`, `target=start`, 嚴格 `<` 更新 → lastSeen 最小、平手取小號。後端:`min(range(lo,hi+1), key=lambda i:(last_seen[i], i))`(zone34.py:46-48)完全等價。`last_seen` 初值 -1、掃描中記錄最後出現 index,一致。✓
- 細節:後端 `last_seen` 只在 `1<=n<=39` 才寫(zone34.py:33-35),原檔無此防呆但資料保證 1~39,且只查 1~39 的鍵 → 無行為差異。分區計數 (`zones`) 兩邊都不設上界防呆,一致。✓

### [OK] 分區邊界
01-09 / 10-19 / 20-29 / 30-39,第 1 柱 9 顆、其餘 10 顆,兩邊相同。✓

### [OK] 徽章文字與策略 A 標題
`進場時刻` / `觀望中` / `重擊進場`(Zone34Panel.tsx:59,91;zone34.py 隱含由 active 決定)與原檔 html:265,275,282,286 一致。策略 A 標題「🔥 達標!建議刪除極端死號:」逐字相同(tsx:67 vs html:96)。✓

---

## 呈現/文案落差

### [INTENTIONAL] 雙引擎並排 → 改為單一遊戲切換器 · 中
原站同時渲染 **539 + 天天樂兩個面板**(html:79-146,雙 panel 疊放)。我方 `YahongView` 全 5 個雅宏子分頁共用**一個遊戲切換鈕**(YahongView.tsx:52-67),`Zone34Panel` 只吃當前 `gameKey`(tsx:118),一次只顯示一個遊戲。
- 後端 `radar` 端點**兩個遊戲都取得到**(`?game=lotto539` / `?game=fantasy5`),只是前端每次只叫一個。
- 判定:這是**整個雅宏區的一致性重設計**(五分頁皆單遊戲切換),非遺漏。但使用者失去「一眼並看兩盤連槓」的體驗。
- 修法(若要對齊):Zone34 子分頁改為同時 `useAsync(yahongRadar('lotto539'))` + `yahongRadar('fantasy5')`,並排兩張卡;或維持現狀(建議,與其他分頁一致)。

### [MISSING] 策略 B 文案漏「保本防呆,」前綴 · 低
原檔打法字串:`保本防呆,四區 1200 碰全包。`(html:103,139)。後端 note 只給 `四區 1200 碰全包`(zone34.py:72),漏掉「保本防呆,」。
- 修法:zone34.py:72 note 改 `保本防呆,四區 1200 碰全包`。

### [COSMETIC] 兩策略說明都漏「打法:」標籤 · 低
原檔卡片說明格式為「條件:連 N 期未出同區 3 顆。**打法:**〔文案〕」(html:94,103,130,139),打法二字加粗。我方渲染成「條件:連 N 期未出同區 3 顆。〔note〕」(tsx:62-64,94-96),沒有「打法:」標籤與粗體。
- 修法:tsx 說明模板改 `條件:連 {threshold} 期未出同區 3 顆。打法:{note}`(或 note 前置)。

### [COSMETIC] 策略 B 達標文案漏尾句「睡覺免煩惱!」 · 低
原檔:`🎯 狙擊時機已到!今晚不刪牌直接滿注進場,等待爆發,睡覺免煩惱!`(html:105-106)。我方:`🎯 狙擊時機已到!今晚不刪牌直接滿注進場,等待爆發。`(tsx:99),漏尾句。
- 修法:tsx:99 補回「,睡覺免煩惱!」。

### [COSMETIC] 策略 A 冷號未標各柱名 · 低
原檔達標時列四行「第1柱 (01-09):07」(柱名+號碼,html:268-271)。我方在策略 A 達標區只放四顆號碼圓標、無柱名(tsx:69-73);柱名+號碼的對應改由上方**獨立「四柱最冷號」卡**呈現(tsx:33-45,順序相同)。資訊未遺失,僅版面改組。
- 修法:若要對齊,達標區每顆號碼補上 `columns[i].label`,或以現有「四柱最冷號」卡代替(建議)。

### [COSMETIC / INTENTIONAL] 「四柱最冷號」卡恆顯示(原站僅達標才露號) · 低
原站四柱冷號**只在策略 A `>=5` 達標時**出現(html:95-98 預設 `display:none`)。我方新增的「四柱最冷號」卡只要有 columns 就常駐顯示(tsx:33),等於未達標也先曝光「死號」。屬新增輔助資訊,無害但與原行為不同。
- 修法:若要對齊,將該卡包在 `data.strategyA.active` 條件下;或保留現狀作為輔助。

### [COSMETIC] 達標高亮少了脈動動畫 · 低
原檔達標卡 `active-a`(紅)/`active-b`(紫)含徽章 `pulse` 動畫(html:42-47)。我方以背景/邊框變色表達達標(tsx:48-49,80-81),無脈動。視覺弱一點,不影響資訊。
- 修法:給徽章加 `animate-pulse`(可選)。

---

## 其他查核(無落差)

- **今日開出標記 / 數字動畫**:原站本來就**沒有**「今日開出」標記,數字也無入場動畫(僅達標脈動,已列上),故無可遺漏。[OK]
- **資料上限**:`radar_ep` 用**全量**歷史(未套 `MAX_DRAWS=1500`,yahong.py:47),與原站吃 Firebase 全量一致;且 consecutiveMiss 為尾端連槓、coldest 亦不受截斷利弊影響。[OK]
- **雙位補零**:`pad2`(tsx:40,71)對應原檔 `padStart(2,'0')`。[OK]

---

## 總結

- **落差總數:7**(其中 [INTENTIONAL] 2、[MISSING] 1、[COSMETIC] 4;[BUG] 0)。
- **最重要 3 個**:
  1. [INTENTIONAL] 雙引擎並排改單一遊戲切換 —— 唯一「功能面」差異,是否要對齊需產品決定(建議維持,與其餘四分頁一致)。
  2. [MISSING] 策略 B note 漏「保本防呆,」前綴(zone34.py:72)—— 唯一的後端文案缺字,一行可補。
  3. [COSMETIC] 策略 B 達標文案漏尾句「睡覺免煩惱!」(tsx:99)+ 兩策略漏「打法:」標籤 —— 文案完整度。
- 核心數學(連槓計數、5/7 門檻、四柱最冷號、平手取小號)**完全一致**,可放心。
